// *** WARNING: this file was generated by pulumi-language-dotnet. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

using System;
using System.Collections.Immutable;

namespace Pulumi.Redpanda
{
    public static class Config
    {
        [global::System.Diagnostics.CodeAnalysis.SuppressMessage("Microsoft.Design", "IDE1006", Justification = 
        "Double underscore prefix used to avoid conflicts with variable names.")]
        private sealed class __Value<T>
        {
            private readonly Func<T> _getter;
            private T _value = default!;
            private bool _set;

            public __Value(Func<T> getter)
            {
                _getter = getter;
            }

            public T Get() => _set ? _value : _getter();

            public void Set(T value)
            {
                _value = value;
                _set = true;
            }
        }

        private static readonly global::Pulumi.Config __config = new global::Pulumi.Config("redpanda");

        private static readonly __Value<string?> _accessToken = new __Value<string?>(() => __config.Get("accessToken"));
        /// <summary>
        /// Redpanda client token. You need either `AccessToken`, or both `ClientId` and `ClientSecret` to use this provider. Can also be set with the `REDPANDA_ACCESS_TOKEN` environment variable.
        /// </summary>
        public static string? AccessToken
        {
            get => _accessToken.Get();
            set => _accessToken.Set(value);
        }

        private static readonly __Value<string?> _azureClientId = new __Value<string?>(() => __config.Get("azureClientId"));
        /// <summary>
        /// Used for creating and managing BYOC and BYOVPC clusters. Can also be specified in the environment as AZURE_CLIENT_ID or ARM_CLIENT_ID
        /// </summary>
        public static string? AzureClientId
        {
            get => _azureClientId.Get();
            set => _azureClientId.Set(value);
        }

        private static readonly __Value<string?> _azureClientSecret = new __Value<string?>(() => __config.Get("azureClientSecret"));
        /// <summary>
        /// Used for creating and managing BYOC and BYOVPC clusters. Can also be specified in the environment as AZURE_CLIENT_SECRET or ARM_CLIENT_SECRET
        /// </summary>
        public static string? AzureClientSecret
        {
            get => _azureClientSecret.Get();
            set => _azureClientSecret.Set(value);
        }

        private static readonly __Value<string?> _azureSubscriptionId = new __Value<string?>(() => __config.Get("azureSubscriptionId"));
        /// <summary>
        /// The default Azure Subscription ID which should be used for Redpanda BYOC clusters. If another subscription is specified on a resource, it will take precedence. This can also be sourced from the `ARM_SUBSCRIPTION_ID` environment variable.
        /// </summary>
        public static string? AzureSubscriptionId
        {
            get => _azureSubscriptionId.Get();
            set => _azureSubscriptionId.Set(value);
        }

        private static readonly __Value<string?> _azureTenantId = new __Value<string?>(() => __config.Get("azureTenantId"));
        /// <summary>
        /// Used for creating and managing BYOC and BYOVPC clusters. Can also be specified in the environment as AZURE_TENANT_ID or ARM_TENANT_ID
        /// </summary>
        public static string? AzureTenantId
        {
            get => _azureTenantId.Get();
            set => _azureTenantId.Set(value);
        }

        private static readonly __Value<string?> _clientId = new __Value<string?>(() => __config.Get("clientId"));
        /// <summary>
        /// The ID for the client. You need either `ClientId` AND `ClientSecret`, or `AccessToken`, to use this provider. Can also be set with the `REDPANDA_CLIENT_ID` environment variable.
        /// </summary>
        public static string? ClientId
        {
            get => _clientId.Get();
            set => _clientId.Set(value);
        }

        private static readonly __Value<string?> _clientSecret = new __Value<string?>(() => __config.Get("clientSecret"));
        /// <summary>
        /// Redpanda client secret. You need either `ClientId` AND `ClientSecret`, or `AccessToken`, to use this provider. Can also be set with the `REDPANDA_CLIENT_SECRET` environment variable.
        /// </summary>
        public static string? ClientSecret
        {
            get => _clientSecret.Get();
            set => _clientSecret.Set(value);
        }

        private static readonly __Value<string?> _gcpProjectId = new __Value<string?>(() => __config.Get("gcpProjectId"));
        /// <summary>
        /// The default Google Cloud Project ID to use for Redpanda BYOC clusters. If another project is specified on a resource, it will take precedence. This can also be sourced from the `GOOGLE_PROJECT` environment variable, or any of the following ordered by precedence: `GOOGLE_PROJECT`, `GOOGLE_CLOUD_PROJECT`, `GCLOUD_PROJECT`, or `CLOUDSDK_CORE_PROJECT`.
        /// </summary>
        public static string? GcpProjectId
        {
            get => _gcpProjectId.Get();
            set => _gcpProjectId.Set(value);
        }

        private static readonly __Value<string?> _googleCredentials = new __Value<string?>(() => __config.Get("googleCredentials"));
        /// <summary>
        /// Used for creating and managing BYOC and BYOVPC clusters. Can also be specified in the environment as GOOGLE_CREDENTIALS
        /// </summary>
        public static string? GoogleCredentials
        {
            get => _googleCredentials.Get();
            set => _googleCredentials.Set(value);
        }

        private static readonly __Value<string?> _googleCredentialsBase64 = new __Value<string?>(() => __config.Get("googleCredentialsBase64"));
        /// <summary>
        /// Used for creating and managing BYOC and BYOVPC clusters. Is a convenience passthrough for base64 encoded credentials intended for use in CI/CD. Can also be specified in the environment as GOOGLE_CREDENTIALS_BASE64
        /// </summary>
        public static string? GoogleCredentialsBase64
        {
            get => _googleCredentialsBase64.Get();
            set => _googleCredentialsBase64.Set(value);
        }

    }
}
